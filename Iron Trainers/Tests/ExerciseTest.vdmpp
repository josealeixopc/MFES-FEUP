class ExerciseTest is subclass of Test
types
-- TODO Define types here
values
-- TODO Define values here
instance variables
	
	testName : seq of char := "testName";
	testDescription: seq of char := "testDescription";
	testBodyPart: Exercise`BodyPart := <Arm>;
	
	e1 : Exercise := new Exercise(testName, testDescription, testBodyPart);

operations

	private testConstructor: () ==> ()
	testConstructor() == 
	(
		assertEquals(e1.getName(), testName);
		assertEquals(e1.getDescription(), testDescription);
		assertEquals(e1.getBodyPart(), testBodyPart);
	);
	
	public static run: () ==> ()
 	run() ==
 	(
 		new ExerciseTest().testConstructor();
 	);

functions
-- TODO Define functiones here
traces
-- TODO Define Combinatorial Test Traces here
end ExerciseTest