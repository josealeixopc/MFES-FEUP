class Test
types
-- TODO Define types here
values
-- TODO Define values here
instance variables
-- TODO Define instance variables here
operations

	-- fails if expected is not equal to actual	
	protected assertEquals: ? * ? ==> ()
	assertEquals(expected, actual) == return
	pre expected = actual;
	
	protected assertNotEquals: ? * ? ==> ()
	assertNotEquals(expected, actual) == return
	pre expected <> actual;
	
	public static run: () ==> ()
 	run() ==
 	(
 		UserTest`run();
 		TrainerTest`run();
 		ClientTest`run();
 		
 		MilestoneTest`run();
 		
 		ExerciseTest`run();
 		SeriesTest`run();
 		SetTest`run();
 		
 		IronTrainersTest`run();
 	);
		
functions
-- TODO Define functiones here
traces
-- TODO Define Combinatorial Test Traces here
end Test